The program loads the image specified by the user into bitmap property and creates a grid filled with buttons based on the pixels of an image. After clicking a button the bitmap changes and the grid is reloaded. After clicking save, the bitmap is exported to the file that is specified at the top. After clicking the load flipped button, the bitmap is split into rows, reversed and concatenated with the dimensions before being displayed. 
